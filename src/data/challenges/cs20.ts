import type { CodeChallenge } from '../../types'

/**
 * Code Lab challenges for Computer Science 20, keyed by unit id.
 * Concept tags must match the unit's concept list so results feed the
 * adaptive engine. No input() — pass values via variables in the starter.
 * (No challenges for 'files-data': the browser runner has no filesystem.)
 */
export const cs20Challenges: Record<string, CodeChallenge[]> = {
  'languages-tour': [
    {
      id: 'cs20-lt-c1',
      concept: 'syntax-comparison',
      difficulty: 1,
      title: 'Java loop, Python style',
      task: 'The comment shows a Java counting loop. Write the Python equivalent so it prints the same numbers, one per line.',
      starter: '# Java version:\n#   for (int i = 1; i <= 4; i++) {\n#       System.out.println(i);\n#   }\n# Write the Python version below\n',
      expectedOutput: '1\n2\n3\n4',
      solution: 'for i in range(1, 5):\n    print(i)',
      hint: 'Java runs while i <= 4, so Python needs range(1, 5) — the end value of range() is not included.',
    },
    {
      id: 'cs20-lt-c2',
      concept: 'syntax-comparison',
      difficulty: 2,
      title: 'JavaScript while, Python style',
      task: 'The comment shows a JavaScript while loop. Translate it into Python so the output matches exactly.',
      starter: '# JavaScript version:\n#   let n = 10;\n#   while (n > 0) {\n#       console.log(n);\n#       n = n - 3;\n#   }\n# Write the Python version below\n',
      expectedOutput: '10\n7\n4\n1',
      solution: 'n = 10\nwhile n > 0:\n    print(n)\n    n = n - 3',
      hint: 'Python has while too — drop the parentheses, braces and semicolons, and use indentation for the body.',
    },
    {
      id: 'cs20-lt-c3',
      concept: 'reading-code',
      difficulty: 3,
      title: 'Decode the Java accumulator',
      task: 'First READ the Java code in the comment and work out what it computes. Then write a Python program that produces the same single line of output.',
      starter: '# Java version:\n#   int total = 0;\n#   for (int i = 1; i <= 5; i++) {\n#       total = total + i * i;\n#   }\n#   System.out.println(total);\n# Write the Python version below\n',
      expectedOutput: '55',
      solution: 'total = 0\nfor i in range(1, 6):\n    total = total + i * i\nprint(total)',
      hint: 'It adds i * i for i = 1..5 — the sum of squares: 1 + 4 + 9 + 16 + 25. Print only the final total, after the loop.',
    },
  ],
  'functions-scope': [
    {
      id: 'cs20-fn-c1',
      concept: 'defining-functions',
      difficulty: 1,
      title: 'A reusable greeting',
      task: 'Complete the function greet(name) so it prints a line like:  Hello, Ava!  — The tests call it with several different names, so build the message from the parameter.',
      starter: 'def greet(name):\n    # print:  Hello, <name>!\n    pass\n',
      harness: 'greet("Ava")\ngreet("Ben")',
      expectedOutput: 'Hello, Ava!\nHello, Ben!',
      solution: 'def greet(name):\n    print("Hello, " + name + "!")',
      hint: 'Inside the function, name holds whatever the caller passed in. An f-string works nicely: f"Hello, {name}!"',
    },
    {
      id: 'cs20-fn-c2',
      concept: 'return-values',
      difficulty: 1,
      title: 'Return the square',
      task: 'Complete square(n) so it RETURNS n multiplied by itself (do not print inside the function). The tests print the result of calling it with several inputs.',
      starter: 'def square(n):\n    # return n squared\n    pass\n',
      harness: 'print(square(3))\nprint(square(10))\nprint(square(-4))',
      expectedOutput: '9\n100\n16',
      solution: 'def square(n):\n    return n * n',
      hint: 'return hands the value back to the caller: return n * n. The test code does the printing for you.',
    },
    {
      id: 'cs20-fn-c3',
      concept: 'parameters-arguments',
      difficulty: 2,
      title: 'Area of a rectangle',
      task: 'Complete area(width, height) so it returns the area. Arguments match parameters by position — the tests call it with several width/height pairs and print each result.',
      starter: 'def area(width, height):\n    # return the area of a width x height rectangle\n    pass\n',
      harness: 'print(area(3, 4))\nprint(area(10, 2))\nprint(area(7, 7))',
      expectedOutput: '12\n20\n49',
      solution: 'def area(width, height):\n    return width * height',
      hint: 'Multiply the two parameters and return the result: return width * height.',
    },
    {
      id: 'cs20-fn-c4',
      concept: 'return-values',
      difficulty: 2,
      title: 'The bigger of two',
      task: 'Complete bigger(a, b) so it returns whichever argument is larger (no fair using the built-in max). The tests call it with several pairs, including negatives.',
      starter: 'def bigger(a, b):\n    # return the larger of a and b — use an if/else\n    pass\n',
      harness: 'print(bigger(3, 8))\nprint(bigger(12, 5))\nprint(bigger(-2, -7))',
      expectedOutput: '8\n12\n-2',
      solution: 'def bigger(a, b):\n    if a > b:\n        return a\n    else:\n        return b',
      hint: 'if a > b: return a — otherwise return b. Remember −2 is LARGER than −7.',
    },
    {
      id: 'cs20-fn-c5',
      concept: 'scope',
      difficulty: 3,
      title: 'Trapped in scope',
      task: 'The function computes the right answer but the tests print None, because result is a LOCAL variable that never leaves the function. Fix it so the doubled value gets back to the caller.',
      starter: 'def double(n):\n    result = n * 2\n    # result is local to this function — it vanishes when the function ends.\n    # Get the value back to the caller!\n',
      harness: 'print(double(5))\nprint(double(12))',
      expectedOutput: '10\n24',
      solution: 'def double(n):\n    result = n * 2\n    return result',
      hint: 'Local variables die when the function returns. The escape hatch is the return statement: return result.',
    },
  ],
  'lists-strings': [
    {
      id: 'cs20-ls-c1',
      concept: 'list-basics',
      difficulty: 1,
      title: 'First and last',
      task: 'Using INDEXING (not by retyping the words), print the first colour on one line and the last colour on the next.',
      starter: 'colours = ["red", "green", "blue", "yellow"]\n# print the first, then the last — use indices\n',
      expectedOutput: 'red\nyellow',
      solution: 'colours = ["red", "green", "blue", "yellow"]\nprint(colours[0])\nprint(colours[-1])',
      hint: 'Indexing starts at 0, and negative indices count from the end: colours[-1] is the last element.',
    },
    {
      id: 'cs20-ls-c2',
      concept: 'list-methods',
      difficulty: 2,
      title: 'Grow and shrink',
      task: 'Modify the list using list METHODS: add 16 to the end, remove the value 8, then print the whole list once.',
      starter: 'nums = [4, 8, 15]\n# append 16, remove 8, then print the list\n',
      expectedOutput: '[4, 15, 16]',
      solution: 'nums = [4, 8, 15]\nnums.append(16)\nnums.remove(8)\nprint(nums)',
      hint: 'append(x) adds to the end; remove(x) deletes by VALUE. Then print(nums) shows the list in [ ] form.',
    },
    {
      id: 'cs20-ls-c3',
      concept: 'string-processing',
      difficulty: 2,
      title: 'Word count and shout',
      task: 'Print two lines: first the NUMBER of words in the sentence, then the whole sentence in capitals. Use string methods — it must work for any sentence.',
      starter: 'sentence = "the quick brown fox"\n# line 1: how many words   line 2: the sentence in CAPITALS\n',
      expectedOutput: '4\nTHE QUICK BROWN FOX',
      solution: 'sentence = "the quick brown fox"\nprint(len(sentence.split()))\nprint(sentence.upper())',
      hint: 'split() turns the sentence into a list of words — len() of that list is the word count. upper() returns the capitals version.',
    },
    {
      id: 'cs20-ls-c4',
      concept: 'slicing',
      difficulty: 2,
      title: 'Slice it up',
      task: 'Using SLICES, print the first three letters of the word on one line and the last three letters on the next.',
      starter: 'word = "computer"\n# use slicing — no retyping letters\n',
      expectedOutput: 'com\nter',
      solution: 'word = "computer"\nprint(word[:3])\nprint(word[-3:])',
      hint: 'word[:3] takes indices 0, 1, 2. A negative start counts from the end: word[-3:] is the last three characters.',
    },
    {
      id: 'cs20-ls-c5',
      concept: 'iterate-collections',
      difficulty: 3,
      title: 'How many passed?',
      task: 'Loop over the marks and count how many are 60 or higher, then print the count once. It should work for any list of marks.',
      starter: 'marks = [72, 55, 90, 48, 66]\n# count the marks that are >= 60, then print the count\n',
      expectedOutput: '3',
      solution: 'marks = [72, 55, 90, 48, 66]\ncount = 0\nfor m in marks:\n    if m >= 60:\n        count = count + 1\nprint(count)',
      hint: 'The accumulator pattern with a condition: start count at 0, add 1 inside the if, and print AFTER the loop.',
    },
  ],
  'logic-nested': [
    {
      id: 'cs20-ln-c1',
      concept: 'boolean-operators',
      difficulty: 1,
      title: 'Can you ride?',
      task: 'The ride requires height at least 120 cm AND age at least 8. Print "can ride" or "cannot ride" using ONE if/else with a compound condition. It must work for any height and age.',
      starter: 'height = 125\nage = 9\n# one if/else with and\n',
      expectedOutput: 'can ride',
      solution: 'height = 125\nage = 9\nif height >= 120 and age >= 8:\n    print("can ride")\nelse:\n    print("cannot ride")',
      hint: 'Join the two comparisons with and — both must be True for the whole condition to be True.',
    },
    {
      id: 'cs20-ln-c2',
      concept: 'nested-ifs',
      difficulty: 2,
      title: 'Letter grade ladder',
      task: 'Print the letter grade for the score: 80+ is A, 65-79 is B, 50-64 is C, below 50 is F. Use a chain of conditions that works for ANY score.',
      starter: 'score = 85\n# if / elif / elif / else\n',
      expectedOutput: 'A',
      solution: 'score = 85\nif score >= 80:\n    print("A")\nelif score >= 65:\n    print("B")\nelif score >= 50:\n    print("C")\nelse:\n    print("F")',
      hint: 'Check from the TOP grade down: once a condition matches, the rest are skipped, so each elif only needs its lower bound.',
    },
    {
      id: 'cs20-ln-c3',
      concept: 'truth-tables',
      difficulty: 2,
      title: 'Print a truth table',
      task: 'Use two nested loops over [True, False] to print the full truth table for AND — each line shows a, b, and a and b, separated by spaces (4 lines total).',
      starter: '# for a in [True, False]:\n#     for b in ...\n',
      expectedOutput: 'True True True\nTrue False False\nFalse True False\nFalse False False',
      solution: 'for a in [True, False]:\n    for b in [True, False]:\n        print(a, b, a and b)',
      hint: 'print(a, b, a and b) puts single spaces between the values automatically. The nested loops generate all 4 rows: TT, TF, FT, FF.',
    },
    {
      id: 'cs20-ln-c4',
      concept: 'nested-loops',
      difficulty: 3,
      title: 'Mini multiplication grid',
      task: 'Use NESTED loops to print a 3 by 3 multiplication grid: row i contains i*1, i*2, i*3 separated by spaces, for i from 1 to 3.',
      starter: '# outer loop = rows, inner loop = columns\n# tip: print(x, end=" ") stays on the same line; print() ends the line\n',
      expectedOutput: '1 2 3\n2 4 6\n3 6 9',
      solution: 'for i in range(1, 4):\n    for j in range(1, 4):\n        print(i * j, end=" ")\n    print()',
      hint: 'The inner loop prints i * j with end=" " to stay on one line; after it finishes, a bare print() moves to the next row.',
    },
  ],
  'searching-sorting': [
    {
      id: 'cs20-ss-c1',
      concept: 'linear-search',
      difficulty: 1,
      title: 'Is it in the list?',
      task: 'Use a loop to scan the list for the target. Print "found" if the target appears, otherwise "not found". It must work for any list and target.',
      starter: 'items = [12, 7, 30, 5]\ntarget = 30\n# scan with a loop, then print found / not found\n',
      expectedOutput: 'found',
      solution: 'items = [12, 7, 30, 5]\ntarget = 30\nfound = False\nfor item in items:\n    if item == target:\n        found = True\nif found:\n    print("found")\nelse:\n    print("not found")',
      hint: 'Keep a boolean flag that starts False and flips to True when you see the target. Print once, AFTER the loop.',
    },
    {
      id: 'cs20-ss-c2',
      concept: 'algorithm-comparison',
      difficulty: 2,
      title: 'Count the comparisons',
      task: 'Linear search checks elements one by one. Count how many comparisons it makes before finding the target (count the successful check too), and print that number.',
      starter: 'items = [3, 9, 7, 1, 8]\ntarget = 1\n# count every comparison, stop when found, print the count\n',
      expectedOutput: '4',
      solution: 'items = [3, 9, 7, 1, 8]\ntarget = 1\ncount = 0\nfor item in items:\n    count = count + 1\n    if item == target:\n        break\nprint(count)',
      hint: 'Add 1 to the counter BEFORE testing each element, and break out of the loop the moment the target matches.',
    },
    {
      id: 'cs20-ss-c3',
      concept: 'sorting',
      difficulty: 2,
      title: 'One bubble pass',
      task: 'Perform exactly ONE left-to-right pass of bubble sort on the list: compare each neighbouring pair and swap when out of order. Then print the list — it will not be fully sorted yet!',
      starter: 'nums = [5, 3, 8, 1]\n# one pass: compare nums[i] and nums[i+1], swap if needed\n',
      expectedOutput: '[3, 5, 1, 8]',
      solution: 'nums = [5, 3, 8, 1]\nfor i in range(len(nums) - 1):\n    if nums[i] > nums[i + 1]:\n        nums[i], nums[i + 1] = nums[i + 1], nums[i]\nprint(nums)',
      hint: 'Loop i from 0 to len - 2. Python swaps in one line: nums[i], nums[i+1] = nums[i+1], nums[i]. After one pass the largest value (8) reaches the end.',
    },
    {
      id: 'cs20-ss-c4',
      concept: 'binary-search',
      difficulty: 3,
      title: 'Build binary search',
      task: 'Complete binary_search(items, target) for a SORTED list: repeatedly check the middle, discard the wrong half, and return the index of the target — or -1 if it is missing. The tests call it with several targets.',
      starter: 'def binary_search(items, target):\n    low = 0\n    high = len(items) - 1\n    # while low <= high: check the middle, then move low or high\n    return -1\n',
      harness: 'print(binary_search([2, 5, 8, 11, 14, 17, 20], 11))\nprint(binary_search([2, 5, 8, 11, 14, 17, 20], 5))\nprint(binary_search([2, 5, 8, 11, 14, 17, 20], 4))',
      expectedOutput: '3\n1\n-1',
      solution: 'def binary_search(items, target):\n    low = 0\n    high = len(items) - 1\n    while low <= high:\n        mid = (low + high) // 2\n        if items[mid] == target:\n            return mid\n        elif items[mid] < target:\n            low = mid + 1\n        else:\n            high = mid - 1\n    return -1',
      hint: 'mid = (low + high) // 2 (integer division!). Too small? low = mid + 1. Too big? high = mid - 1. Loop while low <= high; if it ends, return -1.',
    },
  ],
}
